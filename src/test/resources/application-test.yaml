spring:
  cache:
    type: none
  init:
    mode: always
  datasource:
    url: jdbc:postgresql://localhost:5433/jira-test
    username: jira
    password: JiraRush
  liquibase:
    enabled: true
    change-log: classpath:db/changelog.sql
    drop-first: true

