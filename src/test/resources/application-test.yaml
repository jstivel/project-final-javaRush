spring:

  sql:
    init:
      mode: never


  liquibase:
    enabled: true
    change-log: classpath:changelog-test.sql

  mail:
    host: localhost
    port: 1025
    username: test
    password: test
